[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "resolution_week1_ConsciousBone"
version = "1"
dependencies = []

[project.scripts]
todo = "resolution_week1_ConsciousBone.main:main"